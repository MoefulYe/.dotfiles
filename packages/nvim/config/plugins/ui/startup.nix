{ lib, lite, ... }:
{
  # Disabled: startup.nvim configuration format has changed in nixvim
  # The plugin expects different structure than what we have
  # TODO: Update to new nixvim startup plugin format or use alternative dashboard

  # Alternative: Use alpha-nvim or dashboard-nvim instead
}
